name: honeycomb_flutter_ui
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  honeycomb:
    path: ../..
  battery_plus: ^6.2.1
  device_info_plus: ^10.1.2
  flutter_background: ^1.3.0+1
  network_info_plus: ^6.1.4
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
